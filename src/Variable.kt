fun main() {
    // Variable constant (not editable)
    val valVariable: Int = 10

    // Variable can editable
    var varVariable: Int = 20
    varVariable += 10

    println(valVariable)
    println(varVariable)
}